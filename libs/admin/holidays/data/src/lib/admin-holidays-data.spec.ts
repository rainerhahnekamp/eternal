import { adminHolidaysData } from './admin-holidays-data';

describe('adminHolidaysData', () => {
  it('should work', () => {
    expect(adminHolidaysData()).toEqual('admin-holidays-data');
  });
});

import { Routes } from '@angular/router';
import { HolidaysComponent } from '../../../ui/src/lib/holidays/holidays.component';

export const adminHolidaysRoutes: Routes = [
  { path: '', component: HolidaysComponent },
];

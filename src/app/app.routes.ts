import { Routes } from '@angular/router';
import { SewerLocatorComponent } from './sewer-finder/sewer-locator.component';

export const appRoutes: Routes = [
  {
    path: '',
    component: SewerLocatorComponent,
  },
];
